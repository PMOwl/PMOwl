<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Connection Name
    |--------------------------------------------------------------------------
    */

    'default' => 'main',

    /*
    |--------------------------------------------------------------------------
    | Middleware...
    |--------------------------------------------------------------------------
    */
   'middleware' => [
        'open' => true,

        // 路由中需要被 decode 的 id
        'route_parameters' => [
            'public_id',
            'topic_id',
        ],

        // 请求参数需要被 decode 的 id
        'request_parameters' => [
            'public_id',
            'topic_id',
        ]
   ],

   /*
    |--------------------------------------------------------------------------
    | Strict Mode
    |--------------------------------------------------------------------------
    | 严格模式下，trudId 失败会直接抛出异常，否者返回 0.
    */
    'strict' => [
        'enable' => true,
        'default' => 0,
    ],

    /*
    |--------------------------------------------------------------------------
    | Hashids Connections
    |--------------------------------------------------------------------------
    */

    'connections' => [

        'main' => [
            'prefix' => '',
            'salt' => 'pmowl',
            'length' => '24',
            'alphabet' => shuffle_str(),
        ],

        'alternative' => [
            'prefix' => '',
            'salt' => 'your-salt-string',
            'length' => 'your-length-integer',
            'alphabet' => 'your-alphabet-string',
        ],

    ],

];
